const useStyles = makeStyles((theme) => ({
  // FileUploadStatus(input, error, done, loading, idle)
  textField: {
    marginLeft: theme.spacing(1),
    marginRight: theme.spacing(1),
    border: "1px solid red",
  },
}));
