export class CellReference {
  constructor(type, instructor, studio, row, column) {
    this.type = type;
    this.instructor = instructor;
    this.studio = studio;
    this.row = row;
    this.column = column;
  }
}
